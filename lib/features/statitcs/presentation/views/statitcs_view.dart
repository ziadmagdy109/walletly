import 'package:flutter/material.dart';

class StatitcsView extends StatelessWidget {
  const StatitcsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Statitcs"));
  }
}
